class DragTracker {
  constructor(registry) {
    this.registry = registry
    this.progress = false
  }

  cancel() {
  }

  onMouseDown(/* e */) {
  }

  onMouseMove(/* e */) {
  }

  onMouseUp(/* e */) {
  }
}

export default DragTracker
