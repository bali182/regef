export default class EditPolicy {
  constructor() {
    this.host = null
    this.toolkit = null
  }

  perform(/* request */) {
  }

  requestFeedback(/* request */) {
  }

  eraseFeedback(/* request */) {
  }
}
